services:
  apoci:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - apoci-data:/apoci/storage
      - ./configs/apoci.example.yaml:/apoci/config/apoci.yaml:ro

volumes:
  apoci-data:
